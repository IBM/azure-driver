{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "vnet_name": {
            "value": "{{vnet_name}}"
        },
		"subnet_name": {
            "value": "{{subnet_name}}"
        },
		"subnet_cidr_block": {
            "value": "{{subnet_cidr_block}}"
        },
		"subnet_private_network_endpoint_policies": {
            "value": "{{subnet_private_network_endpoint_policies}}"
        },
        "subnet_private_link_service_network_policies": {
            "value": "{{subnet_private_link_service_network_policies}}"
        },
        "route_table_name": {
          "value": "{{route_table_name}}"
        },
        "network_security_group_name": {
          "value": "{{network_security_group_name}}"
        }
    }
}
